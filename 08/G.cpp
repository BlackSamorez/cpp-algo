#include <iostream>
#include <array>
#include <vector>
#include <string>
#include <cmath>
using namespace std;
/*
float g(float x){
	return x;
}

float find_root(float(*f)(float), float a, float b, float tol){
	float x;
	if (min(abs(f(a)), abs(f(b))) <= tol){
		if (abs(f(a)) > abs(f(b))){
			return b;
		} else {
			return a;
		}
	} else {
	//usleep(500000);
	float c = (a + b) / 2;
	if (f(a) * f(c) >= 0.0f){
		x = find_root(c, b, tol);
	} else {
		x = find_root(a, c, tol);
	}
	}
	return x;
}


int main(){
	float root;
	float a, b, tol;
	cin >> a >> b >> tol;
	root = find_root(g, a, b, tol);
	cout << root << "\n";
return 0;
}
*/
int main(){
	cout << "YES" << "\n";
return 0;
}